eval $(docker-machine env Char)
#`docker-machine ls` to check result
# ---------------------------------
# export DOCKER_TLS_VERIFY="1"
# export DOCKER_HOST="tcp://192.168.99.100:2376"
# export DOCKER_CERT_PATH="/Users/omaiko/.docker/machine/machines/Char"
# export DOCKER_MACHINE_NAME="Char"
# # Run this command to configure your shell:
# # eval $(docker-machine env Char)
# # Использование eval выполняет каждую из этих команд export вместо того, чтобы просто записывать их в консоль, поэтому это быстрый способ настройки переменных среды.
